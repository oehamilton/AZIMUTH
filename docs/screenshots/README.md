# Screenshots for AZIMUTH

Add app screenshots here so the main README and GitHub project page can show the UI.



**Suggested captures:**

| File | Description |
|------|-------------|
| `main.png` | Main window: map centered on home, a target set, great-circle and loxodrome lines, bearing/distance in sidebar. |
| `about.png` | _(Optional)_ Help → About dialog. |

**Tips:**

- Use **PNG** for clarity. Keep width around 1200px so the README stays readable; GitHub will scale.
- Capture with a home and target set so the map and results are visible.
- After adding files here, the root [README.md](../../README.md) will display them in the **Screenshots** section.

**GitHub social preview:** In repo **Settings → General → Social preview**, you can upload an image (e.g. crop of `main.png` or a 1280×640 tile) so link previews show the app.
